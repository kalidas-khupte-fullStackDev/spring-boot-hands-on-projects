spring:
  application:
    name: config-server
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config
#          search-locations: file:///D:\Java-Study-work\DecProjects\Udemy-courses\spring-boot-hands-on-projects\kd-ecom-microservices\spring-cloude-config-server\src\main\resources\config
#          search-locations: file:///D:/Java-Study-work/DecProjects/Udemy-courses/spring-boot-hands-on-projects/kd-ecom-microservices/spring-cloude-config-server/src/main/resources/config
#  cloud:
#    config:
#      server:
#        jdbc:
#         sql: "SELECT prop_key, prop_value from properties where application=? and profile=? and label=?"
#        default-label: main
#  datasource:
#    url: jdbc:mysql://localhost:3306/configdb
#    username: root
#    password: MySecretPass@123
#    driver-class-name: com.mysql.cj.jdbc.Driver

#    GIT Based Configuration
#  cloud:
#    config:
#      server:
#        git:
#          uri: https://github.com/kalidas-khupte-fullStackDev/central-config-server-ymls
#          username: ${GIT_USER}
#          password: ${PAT}

management:
  endpoint:
    shutdown:
      access: unrestricted
  endpoints:
    web:
      exposure:
        include: health,info,refresh,shutdown

server:
  port: 8888